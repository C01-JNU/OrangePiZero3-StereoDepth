# ROS2 节点参数
# 控制节点内部开关，不与 global_config.yaml 重合
/**:
  ros__parameters:
    # 是否发布深度图（从视差计算）
    publish_depth: true
    # 是否发布点云（从视差计算）
    publish_pointcloud: true

    # 视差图预处理滤波
    apply_disparity_filter: true      # 是否应用中值滤波
    disparity_filter_size: 5           # 中值滤波窗口大小（奇数）
