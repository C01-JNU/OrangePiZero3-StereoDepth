# OrangePiZero3-StereoDepth 项目函数对照表

## 一、已完成实现的函数（完整实现）

### 1. 配置文件系统 (config_manager.h/cpp)
- `ConfigManager::getInstance()` - 获取单例实例
- `ConfigManager::load(config_path)` - 加载配置文件
- `ConfigManager::reload()` - 重新加载配置
- `ConfigManager::save(config_path)` - 保存配置
- `ConfigManager::getString(key, default)` - 获取字符串值
- `ConfigManager::getInt(key, default)` - 获取整数值
- `ConfigManager::getFloat(key, default)` - 获取浮点值
- `ConfigManager::getBool(key, default)` - 获取布尔值
- `ConfigManager::setString(key, value)` - 设置字符串值
- `ConfigManager::setInt(key, value)` - 设置整数值
- `ConfigManager::setFloat(key, value)` - 设置浮点值
- `ConfigManager::setBool(key, value)` - 设置布尔值
- `ConfigManager::getNode(key)` - 获取YAML节点
- `ConfigManager::hasKey(key)` - 检查键是否存在
- `ConfigManager::getKeys()` - 获取所有键
- `ConfigManager::clear()` - 清空配置
- `ConfigManager::getConfigPath()` - 获取配置路径
- `ConfigManager::isLoaded()` - 检查是否已加载

### 2. 日志系统 (logger.h/cpp) - TODO: 需要完整实现
- `Logger::getInstance()` - 获取单例实例
- `Logger::setLogLevel(level)` - 设置日志级别
- `Logger::enableConsoleOutput(enabled)` - 启用控制台输出
- `Logger::enableFileOutput(enabled, filepath)` - 启用文件输出
- `Logger::log(level, message)` - 记录日志

### 3. 主程序入口 (main.cpp) - 框架已完成
- `main(argc, argv)` - 程序主入口
- `signalHandler(signal)` - 信号处理函数

## 二、需要实现的函数（TODO）

### 1. 立体视觉流水线 (stereo_pipeline.h/cpp)
- `StereoPipeline::initialize()` - 初始化流水线
- `StereoPipeline::processStereoPair(left, right)` - 处理立体图像对
- `StereoPipeline::loadTestImages(path)` - 加载测试图像
- `StereoPipeline::loadNextTestImage()` - 加载下一张测试图像
- `StereoPipeline::loadCalibration(calib_file)` - 加载标定参数
- `StereoPipeline::getDisparityMap()` - 获取视差图
- `StereoPipeline::getDepthMap()` - 获取深度图
- `StereoPipeline::saveDisparityMap(path)` - 保存视差图
- `StereoPipeline::cleanup()` - 清理资源
- `StereoPipeline::hasMoreTestImages()` - 检查是否有更多测试图像
- `StereoPipeline::getCurrentLeftImage()` - 获取当前左图像
- `StereoPipeline::getCurrentRightImage()` - 获取当前右图像

### 2. 摄像头接口 (camera_interface.h/cpp)
- `CameraInterface::initialize()` - 初始化摄像头
- `CameraInterface::captureFrame()` - 捕获一帧
- `CameraInterface::getLeftImage()` - 获取左图像
- `CameraInterface::getRightImage()` - 获取右图像
- `CameraInterface::release()` - 释放摄像头

### 3. 标定加载器 (calibration_loader.h/cpp)
- `CalibrationLoader::loadCalibration(filepath)` - 加载标定文件
- `CalibrationLoader::getCameraParams()` - 获取相机参数
- `CalibrationLoader::isValid()` - 检查标定是否有效

### 4. 视差处理器 (disparity_processor.h/cpp)
- `DisparityProcessor::process(raw_disparity)` - 处理原始视差图
- `DisparityProcessor::applyMedianFilter(disparity, size)` - 应用中值滤波
- `DisparityProcessor::applySpeckleFilter(disparity, window, range)` - 应用斑点滤波
- `DisparityProcessor::fillHoles(disparity, method)` - 填充空洞
- `DisparityProcessor::checkConsistency(left_disp, right_disp)` - 一致性检查

### 5. 深度计算器 (depth_calculator.h/cpp)
- `DepthCalculator::calculate(disparity, params)` - 计算深度图
- `DepthCalculator::disparityToDepth(disparity, focal, baseline)` - 视差转深度
- `DepthCalculator::filterDepth(depth_map)` - 滤波深度图
- `DepthCalculator::normalizeDepth(depth_map)` - 归一化深度图

### 6. Vulkan上下文管理 (vulkan_context.h/cpp)
- `VulkanContext::initialize()` - 初始化Vulkan上下文
- `VulkanContext::createDevice()` - 创建设备
- `VulkanContext::createCommandPool()` - 创建命令池
- `VulkanContext::cleanup()` - 清理资源
- `VulkanContext::getDevice()` - 获取设备
- `VulkanContext::getPhysicalDevice()` - 获取物理设备
- `VulkanContext::getComputeQueue()` - 获取计算队列

### 7. Vulkan设备管理 (vulkan_device.h/cpp)
- `VulkanDevice::findComputeQueue()` - 查找计算队列
- `VulkanDevice::createBuffer(size, usage)` - 创建缓冲区
- `VulkanDevice::createImage(width, height, format)` - 创建图像
- `VulkanDevice::copyBuffer(src, dst)` - 复制缓冲区
- `VulkanDevice::copyImage(src, dst)` - 复制图像

### 8. Vulkan管线管理 (vulkan_pipeline.h/cpp)
- `VulkanPipeline::createComputePipeline(shader)` - 创建计算管线
- `VulkanPipeline::createDescriptorSetLayout()` - 创建描述符集布局
- `VulkanPipeline::createDescriptorSet()` - 创建描述符集
- `VulkanPipeline::bindPipeline(command_buffer)` - 绑定管线
- `VulkanPipeline::dispatch(x, y, z)` - 调度计算着色器

### 9. Vulkan着色器管理 (vulkan_shader.h/cpp)
- `VulkanShader::loadShaderSPIRV(filepath)` - 加载SPIR-V着色器
- `VulkanShader::createShaderModule(code)` - 创建着色器模块
- `VulkanShader::compileGLSLtoSPIRV(source)` - 编译GLSL到SPIR-V

### 10. Vulkan缓冲区管理 (vulkan_buffer.h/cpp)
- `VulkanBuffer::allocate(size, usage)` - 分配缓冲区
- `VulkanBuffer::upload(data, size)` - 上传数据
- `VulkanBuffer::download(data, size)` - 下载数据
- `VulkanBuffer::map()` - 映射内存
- `VulkanBuffer::unmap()` - 解映射内存

### 11. Vulkan图像管理 (vulkan_image.h/cpp)
- `VulkanImage::create(width, height, format)` - 创建图像
- `VulkanImage::upload(pixel_data)` - 上传像素数据
- `VulkanImage::download(pixel_data)` - 下载像素数据
- `VulkanImage::transitionLayout(old_layout, new_layout)` - 转换图像布局

### 12. 立体计算管线 (stereo_compute_pipeline.h/cpp)
- `StereoComputePipeline::initialize()` - 初始化计算管线
- `StereoComputePipeline::computeDisparity(left, right)` - 计算视差
- `StereoComputePipeline::createCensusPipeline()` - 创建Census变换管线
- `StereoComputePipeline::createCostPipeline()` - 创建代价计算管线
- `StereoComputePipeline::createWTAPipeline()` - 创建WTA优化管线
- `StereoComputePipeline::createPostProcessPipeline()` - 创建后处理管线

### 13. Census计算 (census_compute.h/cpp)
- `CensusCompute::compute(left, right)` - 计算Census变换
- `CensusCompute::configure(window_size, threshold)` - 配置参数

### 14. 代价计算 (cost_compute.h/cpp)
- `CostCompute::compute(left_census, right_census)` - 计算代价
- `CostCompute::aggregateCosts(cost_volume)` - 聚合代价

### 15. WTA计算 (wta_compute.h/cpp)
- `WTACompute::optimize(cost_volume)` - WTA优化
- `WTACompute::subpixelRefinement(disparity)` - 子像素优化

### 16. 后处理计算 (postprocess_compute.h/cpp)
- `PostProcessCompute::process(disparity)` - 后处理视差
- `PostProcessCompute::applyMedianFilter(disparity)` - 应用中值滤波
- `PostProcessCompute::applySpeckleFilter(disparity)` - 应用斑点滤波

### 17. 图像工具 (image_utils.h/cpp)
- `ImageUtils::loadImage(path)` - 加载图像
- `ImageUtils::saveImage(image, path)` - 保存图像
- `ImageUtils::convertToGray(image)` - 转换为灰度
- `ImageUtils::resizeImage(image, width, height)` - 调整大小
- `ImageUtils::cropImage(image, roi)` - 裁剪图像

### 18. 文件工具 (file_utils.h/cpp)
- `FileUtils::listFiles(dir, pattern)` - 列出文件
- `FileUtils::createDirectory(path)` - 创建目录
- `FileUtils::fileExists(path)` - 检查文件是否存在
- `FileUtils::readBinaryFile(path)` - 读取二进制文件
- `FileUtils::writeBinaryFile(path, data)` - 写入二进制文件

### 19. 校正工具 (rectify_utils.h/cpp)
- `RectifyUtils::loadCalibration(file)` - 加载标定参数
- `RectifyUtils::initRectifyMap(params)` - 初始化校正映射
- `RectifyUtils::rectifyImage(image, map)` - 校正图像
- `RectifyUtils::rectifyStereoPair(left, right)` - 校正立体图像对

### 20. 计时器 (timer.h/cpp)
- `Timer::start()` - 开始计时
- `Timer::stop()` - 停止计时
- `Timer::elapsedMillis()` - 获取经过的毫秒数
- `Timer::elapsedMicros()` - 获取经过的微秒数

### 21. 性能分析器 (profiler.h/cpp)
- `Profiler::getInstance()` - 获取实例
- `Profiler::start()` - 开始分析
- `Profiler::stop()` - 停止分析
- `Profiler::beginFrame()` - 开始帧
- `Profiler::endFrame()` - 结束帧
- `Profiler::beginSection(name)` - 开始分析段
- `Profiler::endSection(name)` - 结束分析段
- `Profiler::printStats()` - 打印统计信息

## 三、已实现的着色器（GLSL计算着色器）

### 1. Census变换着色器 (census_transform.comp)
- 已完成：完整的Census变换实现

### 2. 代价计算着色器 (cost_computation.comp)
- 已完成：完整的代价计算和聚合

### 3. WTA优化着色器 (wta_optimization.comp)
- 已完成：完整的WTA优化和子像素优化

### 4. 后处理着色器 (post_processing.comp)
- 已完成：完整的中值滤波、空洞填充、斑点滤波

## 四、配置文件中的参数组

### 已完整定义在 global_config.yaml 中的参数：
1. 系统设置 (system)
2. 摄像头设置 (camera)
3. 标定设置 (calibration)
4. 图像路径 (paths)
5. Vulkan立体匹配参数 (vulkan_stereo)
   - 基础参数 (disparity, pyramid_levels)
   - Census变换 (census_transform)
   - 代价计算 (cost_computation)
   - WTA优化 (wta)
   - 后处理 (post_processing)
   - Vulkan特定参数 (vulkan_specific)
6. CPU备用算法参数 (cpu_stereo)
7. 深度图生成参数 (depth)
8. ROS2集成参数 (ros2)
9. 性能优化参数 (performance)
10. 输出配置 (output)

## 五、需要简化的实现（因时间/复杂度限制）

### 1. Vulkan内存管理 - 简化实现
- 使用简单的内存分配策略
- 避免复杂的内存池管理
- 使用同步传输而不是异步

### 2. 性能监控 - 简化实现
- 仅实现基本的帧率统计
- 简化的性能计数器
- 不使用详细的分析数据

### 3. 错误处理 - 简化实现
- 使用返回码而不是异常
- 简化的错误消息
- 基本的资源清理

### 4. 图像金字塔 - 可选实现
- 初始版本可跳过金字塔
- 使用固定尺寸的图像处理

### 5. 多线程 - 简化实现
- 使用OpenMP或简单线程池
- 避免复杂的锁机制

## 六、优先级排序

### 高优先级（核心功能）：
1. Vulkan上下文和设备初始化
2. 着色器编译和管线创建
3. 基本的立体匹配流水线
4. 图像加载和保存
5. 配置和日志系统

### 中优先级（增强功能）：
1. 摄像头接口
2. 标定文件加载
3. 深度计算
4. 后处理优化

### 低优先级（可选功能）：
1. ROS2集成
2. 详细的性能分析
3. 高级的内存管理
4. 图像金字塔处理

这个函数对照表提供了项目的完整蓝图，可以根据优先级逐步实现。